@model IEnumerable<iClinic.Models.LoaiNhanVien>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiNhanVien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiNhanVien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiNhanVien }) @*|
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiNhanVien }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiNhanVien })*@
        </td>
    </tr>
}

</table>
